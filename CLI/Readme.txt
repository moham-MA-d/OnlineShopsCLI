configuration.json:
    load settings from appsettings.json
    
CommandLineParse:
    A package for cli applications.
    
Refit:
    an http client to call API 
    Refit doesn't need implemetation  
    
appsetting.json (properties)
    it is being transfered to bin folder so we don't not load our settings.    